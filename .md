project-root/
├── server/
│   ├── src/
│   │   ├── config/         # db connection, env validation
│   │   ├── models/         # Mongoose schemas (User, CardRecord, etc.)
│   │   ├── controllers/    # route handler logic
│   │   ├── routes/         # express routers
│   │   ├── middleware/     # auth guard, error handler, validation
│   │   ├── types/          # shared TS interfaces
│   │   └── index.ts        # entrypoint
│   ├── tsconfig.json
│   └── package.json
├── client/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── api/            # axios/fetch wrapper functions
│   │   ├── types/
│   │   └── main.tsx
│   ├── tsconfig.json
│   └── package.json
└── docker-compose.yml       # add this in week 3, not now

mongosh "mongodb+srv://cluster0.v3ltu2m.mongodb.net/" --apiVersion 1 --username <db_username> --password OTiU7VkVRZdgRP4v